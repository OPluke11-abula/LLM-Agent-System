---
id: calculate
description: 'Perform a basic arithmetic calculation.

  Supports: add, subtract, multiply, divide.'
version: 1.0.0
inputs:
  operation:
    type: string
    required: true
    description: 'The arithmetic operation to perform. Must be one of: add, subtract,
      multiply, divide.'
  a:
    type: number
    required: true
    description: The first number.
  b:
    type: number
    required: true
    description: The second number.
outputs:
  success: Plain text result string.
  error: String prefixed with Error:.
safety_notes:
- Division by zero returns an error string.
- This skill is deterministic and does not access external systems.
author: LAS Tool Manifest Auto-Sync
---

# calculate

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Perform a basic arithmetic calculation.
Supports: add, subtract, multiply, divide.

## 2. Required Inputs

- `operation` (string, **Required**): The arithmetic operation to perform. Must be one of: add, subtract, multiply, divide.
- `a` (number, **Required**): The first number.
- `b` (number, **Required**): The second number.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Division by zero returns an error string.
- This skill is deterministic and does not access external systems.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/example_skill_template.py`
- Function: `calculate`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `False`

---
Generated by LAS Tool Manifest Auto-Sync.
