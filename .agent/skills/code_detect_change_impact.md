---
id: code_detect_change_impact
name: code_detect_change_impact
description: Summarize likely structural impact for changed files using the local
  code graph.
version: 1.0.0
inputs:
  changed_files:
    type: array
    required: true
    description: Workspace-relative changed file paths.
  limit:
    type: integer
    required: false
    description: Maximum rows per impact category.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Changed file paths are constrained to the workspace.
- Impact summaries are bounded evidence pointers, not proof of complete blast radius.
author: LAS Tool Manifest Auto-Sync
---

# code_detect_change_impact

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Summarize likely structural impact for changed files using the local code graph.

## 2. Required Inputs

- `changed_files` (array, **Required**): Workspace-relative changed file paths.
- `limit` (integer, Optional): Maximum rows per impact category.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Changed file paths are constrained to the workspace.
- Impact summaries are bounded evidence pointers, not proof of complete blast radius.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_detect_change_impact`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
