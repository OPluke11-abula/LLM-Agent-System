---
id: code_get_architecture
name: code_get_architecture
description: Return a compact architecture summary from the local code graph.
version: 1.0.0
inputs:
  limit:
    type: integer
    required: false
    description: Maximum rows per sample section.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Read-only aggregate query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Architecture summaries omit raw config values and require source verification for decisions.
author: LAS Tool Manifest Auto-Sync
---

# code_get_architecture

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Return a compact architecture summary from the local code graph.

## 2. Required Inputs

- `limit` (integer, Optional): Maximum rows per sample section.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Read-only aggregate query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Architecture summaries omit raw config values and require source verification for decisions.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_get_architecture`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
