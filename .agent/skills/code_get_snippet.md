---
id: code_get_snippet
name: code_get_snippet
description: Read a bounded source snippet by symbol or workspace-relative path and
  line.
version: 1.0.0
inputs:
  symbol:
    type: string
    required: false
    description: Symbol name or qualified-name substring.
  path:
    type: string
    required: false
    description: Workspace-relative path.
  line:
    type: string
    required: false
    description: Line number when path is provided.
  context_lines:
    type: integer
    required: false
    description: Lines before and after the target.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Reads bounded source snippets from workspace-contained paths only.
- Snippet size is capped by `context_lines` and runtime limits.
- Do not use this tool to print secrets or full files.
author: LAS Tool Manifest Auto-Sync
---

# code_get_snippet

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Read a bounded source snippet by symbol or workspace-relative path and line.

## 2. Required Inputs

- `symbol` (any, Optional): Symbol name or qualified-name substring.
- `path` (any, Optional): Workspace-relative path.
- `line` (any, Optional): Line number when path is provided.
- `context_lines` (integer, Optional): Lines before and after the target.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Reads bounded source snippets from workspace-contained paths only.
- Snippet size is capped by `context_lines` and runtime limits.
- Do not use this tool to print secrets or full files.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_get_snippet`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
