---
id: code_index_repo
name: code_index_repo
description: Build or refresh the local read-only code graph SQLite index.
version: 1.0.0
inputs:
  root:
    type: string
    required: false
    description: Workspace root to index.
  output:
    type: string
    required: false
    description: Workspace-relative SQLite output path.
  max_file_bytes:
    type: integer
    required: false
    description: Maximum file size to index.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Builds a local SQLite index under a workspace-contained path.
- Does not execute indexed code or install external MCP binaries.
- Config files are indexed by key only; raw config values are not stored.
author: LAS Tool Manifest Auto-Sync
---

# code_index_repo

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Build or refresh the local read-only code graph SQLite index.

## 2. Required Inputs

- `root` (string, Optional): Workspace root to index.
- `output` (string, Optional): Workspace-relative SQLite output path.
- `max_file_bytes` (integer, Optional): Maximum file size to index.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Builds a local SQLite index under a workspace-contained path.
- Does not execute indexed code or install external MCP binaries.
- Config files are indexed by key only; raw config values are not stored.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_index_repo`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
