---
id: code_search_symbol
name: code_search_symbol
description: Search symbols in the local code graph index.
version: 1.0.0
inputs:
  query:
    type: string
    required: true
    description: Symbol name or qualified-name substring.
  kind:
    type: string
    required: false
    description: Optional symbol kind filter.
  limit:
    type: integer
    required: false
    description: Maximum result count.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Graph results are evidence pointers, not authoritative proof.
author: LAS Tool Manifest Auto-Sync
---

# code_search_symbol

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Search symbols in the local code graph index.

## 2. Required Inputs

- `query` (string, **Required**): Symbol name or qualified-name substring.
- `kind` (any, Optional): Optional symbol kind filter.
- `limit` (integer, Optional): Maximum result count.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Graph results are evidence pointers, not authoritative proof.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_search_symbol`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
