---
id: code_trace_call_path
name: code_trace_call_path
description: Trace direct inbound or outbound call edges for a symbol.
version: 1.0.0
inputs:
  symbol:
    type: string
    required: true
    description: Symbol name or qualified-name substring.
  direction:
    type: string
    required: false
    description: Trace outbound calls from the symbol or inbound calls to the symbol.
  limit:
    type: integer
    required: false
    description: Maximum edge count.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Call edges are static evidence pointers and must be verified before security claims.
author: LAS Tool Manifest Auto-Sync
---

# code_trace_call_path

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Trace direct inbound or outbound call edges for a symbol.

## 2. Required Inputs

- `symbol` (string, **Required**): Symbol name or qualified-name substring.
- `direction` (string, Optional): Trace outbound calls from the symbol or inbound calls to the symbol.
- `limit` (integer, Optional): Maximum edge count.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- Read-only query against `.agent/codebase-memory/code_graph.sqlite`.
- Output is bounded by the `limit` argument and runtime cap.
- Call edges are static evidence pointers and must be verified before security claims.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/tool_codebase_memory.py`
- Function: `code_trace_call_path`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `True`

---
Generated by LAS Tool Manifest Auto-Sync.
