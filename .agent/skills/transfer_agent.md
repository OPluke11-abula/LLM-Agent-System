---
id: transfer_agent
name: transfer_agent
description: '[System Tool] Transfer the current session to a different Agent.

  Call this tool ONLY when the user''s request requires a different specialized agent.'
version: 1.0.0
inputs:
  target_agent:
    type: string
    required: true
    description: 'The name of the agent to transfer to. For example: ''CodeEngineer'',
      ''DatabaseAdmin''.'
  reason:
    type: string
    required: true
    description: The reason for the handoff, summarizing what needs to be done next.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- This skill does not execute the target agent by itself.
- Downstream routing and authorization remain runtime responsibilities.
author: LAS Tool Manifest Auto-Sync
---

# transfer_agent

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

[System Tool] Transfer the current session to a different Agent.
Call this tool ONLY when the user's request requires a different specialized agent.

## 2. Required Inputs

- `target_agent` (string, **Required**): The name of the agent to transfer to. For example: 'CodeEngineer', 'DatabaseAdmin'.
- `reason` (string, **Required**): The reason for the handoff, summarizing what needs to be done next.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- This skill does not execute the target agent by itself.
- Downstream routing and authorization remain runtime responsibilities.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/transfer_agent.py`
- Function: `transfer_agent`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `False`

---
Generated by LAS Tool Manifest Auto-Sync.
