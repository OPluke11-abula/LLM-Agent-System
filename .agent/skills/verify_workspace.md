---
id: verify_workspace
name: verify_workspace
description: Verify contracts, lints, and schemas within the active workspace.
version: 1.0.0
inputs:
  sync_first:
    type: boolean
    required: false
    description: Whether to sync tool manifests before validation.
outputs:
  success:
    type: string
    description: Plain text result string.
  error:
    type: string
    description: String prefixed with Error:.
safety_notes:
- This contract is generated from runtime Pydantic reflection.
- Review and harden safety notes before production use.
author: LAS Tool Manifest Auto-Sync
---

# verify_workspace

> **PAP Skill Contract**: This document defines the exact execution boundaries, inputs, and outputs for this skill.

## 1. Purpose

Verify contracts, lints, and schemas within the active workspace.

## 2. Required Inputs

- `sync_first` (boolean, Optional): Whether to sync tool manifests before validation.

## 3. Expected Outputs

- **Success format**: Plain text result string.
- **Error format**: String prefixed with `Error:`.

## 4. Execution Boundaries and Safety

- This contract is generated from runtime Pydantic reflection.
- Review and harden safety notes before production use.

## 5. Runtime Mapping

- Module: `agent_workspace/skills/system_verification.py`
- Function: `verify_workspace`
- Argument model: Pydantic `BaseModel` (see input schema)
- Wants context: `False`

---
Generated by LAS Tool Manifest Auto-Sync.
